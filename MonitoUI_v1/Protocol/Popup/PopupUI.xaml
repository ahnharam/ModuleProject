<Window x:Class="Protocol.Popup.PopupUI"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Protocol.Popup"
        mc:Ignorable="d"
        Title="PopupWindow" Height="300" Width="400" ResizeMode="NoResize" WindowStyle="None">

    <Window.Resources>

    </Window.Resources>
    
    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition />
                            <RowDefinition Height="30"/>
                        </Grid.RowDefinitions>

                        <!-- title bar -->
                        <Grid Grid.Row="0">
                            <Grid.Background>
                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                    <GradientStop Color="#FF3C3D5E" Offset="0"/>
                                    <GradientStop Color="#FF1C1B1F" Offset="1"/>
                                </LinearGradientBrush>
                            </Grid.Background>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0"/>
                            <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" Foreground="White" Margin="20,0,0,0" FontSize="14" FontWeight="Bold"/>
                            <Button Grid.Column="2"/>
                        </Grid>

                        <!-- image -->
                        <Grid Grid.Row="1">

                        </Grid>

                        <!-- info bar -->
                        <Grid Grid.Row="2">

                        </Grid>
                    </Grid>
</Window>
